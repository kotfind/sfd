# Semantic Find

Semantic search over codebase comments.

## Compilation

The project can be built with:

```bash
cargo build
```

Both running and building is possible with:

```bash
cargo run
```

To get help, execute the following:

```bash
cargo run -- --help
```
